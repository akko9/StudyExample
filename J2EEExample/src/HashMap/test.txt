1.HashMap:数组+链表+红黑树
数组插入:
    根据key同行hash算法 算出hashcode,然后取模，得到插入数组的位置将对象(key,value,hashcode,next)插入数组,
    由于hashcode计算时会出现hash冲突,当计算后的hashcode相同,则通过链表的形式将对象插入.
    但由于链表的不断插入,导致链表不断变长,会出现查询效率问题,1.8后加入红黑树,链表len为8时转为红黑树
阈值为8？
    红黑树的插入性能没链表好